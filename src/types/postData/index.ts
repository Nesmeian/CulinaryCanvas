export type PostAuthRegType = {
    email: string;
    login: string;
    password: string;
    firstName?: string;
    lastName?: string;
};
export type PostLoginType = {
    login: string;
    password: string;
};
