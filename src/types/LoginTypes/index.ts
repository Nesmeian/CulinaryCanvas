export type RegFields = {
    firstName: string;
    lastName: string;
    email: string;
    login: string;
    password: string;
    rePassword: string;
};
export type LoginFields = {
    login: string;
    password: string;
};
export type VerifyField = {
    email: string;
};
