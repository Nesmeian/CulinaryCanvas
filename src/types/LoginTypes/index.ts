export type RegFields = {
    name: string;
    lastName: string;
    email: string;
    login: string;
    password: string;
    rePassword: string;
};
export type LoginFields = {
    login: string;
    password: string;
};
